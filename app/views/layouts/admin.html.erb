<!DOCTYPE html>
<html>
  <head>
    <div class="container">
    <title>Startup</title>
	  <%= check_login %>
	  <%= "hi " + current_user.name%>
	  <%= link_to "登出", logout_path(), method: "delete", data: { confirm: "確認登出" } %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    </div>
  </head>

  <body>
    <div class="container">
    <%= notice %>
    </div>
    <div class="container">
    <%= yield %>
    </div>
  </body>
</html>