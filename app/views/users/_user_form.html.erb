  <%= form_for(user) do |f| %>
    <%= f.label :name, t("name") %>
    <%= f.text_field :name %> <br />

    <%= f.label :email, t("view.email") %>
    <%= f.text_field :email %> <br />

    <%= f.label :root, t("view.root") %>
    <%= f.text_field :root %> <br />
	
    <%= f.submit %>
  <% end %>